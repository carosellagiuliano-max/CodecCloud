{
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run -r",
    "e2e": "playwright test",
    "prepare:pr": "node scripts/prepare-pr.mjs"
  }
}
